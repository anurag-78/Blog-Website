
It is a simple bloging website which use MongoDB for storing its blogs.

For UI it uses Simply HTML,CSS and Bootstrap.

For Backend it uses Node.js and express.js.

For templating engine it uses EJS.

